name: Restart Frontend

description: "Here is restart logic for Frontend side."

inputs:
  environment:
    description: "Input environment."
    required: true

runs:
  using: 'composite'
  steps:
    - name: Print Environment From Restart Frontend
      shell: bash
      run: echo "Running Action One in ${{ inputs.environment }}"