name: Deploy Orchestrator

on:
  push:
    branches:
      - main

  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment.'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod

jobs:
  deploy_to_dev:
    # Runs when there is a push event or a manual trigger with 'dev' environment.
    if: ${{github.event_name == 'push' || inputs.environment == 'dev'}}
    uses: ./.github/workflows/deploy_to_dev.yml
    secrets: inherit
    with:
      environment: 'Manual || Push Action = dev'

  deploy_to_prod:
    # Runs only when it triggers manually with 'prod' environment.
    if: inputs.environment == 'prod'
    uses: ./.github/workflows/deploy_to_prod.yml
    secrets: inherit
    with:
      environment: 'Manual triggered prod = prod'

